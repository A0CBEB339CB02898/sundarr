## **phase1 检查结论：已完成**
1. 已完成。SMB 目录浏览结果会经过统一路径规范化处理，输入框中的路径会保持 `/` 前缀，例如 `/movie/xxx`。
2. 已完成。本地媒体库编辑弹窗中已经支持管理“绑定到当前媒体库”的远程媒体库，不再只能在远程媒体库侧单向绑定。
3. 已完成。创建表单不再要求用户手动填写唯一标识，前端会自动生成 UUID。
4. 已完成。任务页不再要求手动输入任务 ID；全局任务列表支持自动轮询刷新，间隔为 5 秒。
5. 已完成。当前页面结构已收口为顶部品牌栏 + 页面主标题区，不再保留重复的大标题/副标题层。
6. 已完成。一级列表页已接入后端分页，默认 `page_size=20`，并提供前端分页切换组件。

## **phase2 修正后结论**
1. 已完成。所有一级页面的列表查询分页能力已落地，本项不再作为待办。
2. 已完成。媒体源、搜索、远程媒体库、本地媒体库、存储、任务等页面已经切换到统一的设计系统组件和页面骨架；按钮、输入框、弹窗和状态展示不再混用旧版页面样式。
3. 已完成。远程媒体库同步成功后，除删除来源文件外，Worker 也会按配置递归清理空目录。
